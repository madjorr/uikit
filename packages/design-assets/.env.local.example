# Copy this file to .env.local and fill in your token. .env.local is gitignored.

# Required: Figma personal access token (Figma → Settings → Personal access tokens)
FIGMA_SYNC_TOKEN=

# Figma file key and the section node that contains all 4 icon pack frames
FIGMA_SYNC_FILE_KEY=lrU3ydIyvPYQNE6ixdsKtJ
FIGMA_SYNC_SECTION_NODE_ID=2893-16552

# Optional: comma-separated platform tags written into each manifest asset (default: PD)
# FIGMA_SYNC_PLATFORMS=PD
