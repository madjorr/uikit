spec_version: '1.0'
component: DropdownMenu

# Framework-agnostic contract for a *composable* menu: a trigger plus a popup of
# items (see anatomy.yaml). The root owns the open state; positioning props live
# on the content; item-level props (checkbox `checked`, radio `value`) live on
# the respective parts.
contract:
  properties:
    - name: open
      type: boolean
      required: false
      description: Controlled open state. Pair with the change event.
    - name: default-open
      type: boolean
      required: false
      default: false
      description: Initial open state when uncontrolled.
    - name: side
      type: "'top' | 'bottom' | 'left' | 'right' | 'inline-start' | 'inline-end'"
      required: false
      default: "'bottom'"
      description: On the content — which side of the trigger to render on.
    - name: align
      type: "'start' | 'center' | 'end'"
      required: false
      description: On the content — alignment along the chosen side.
    - name: side-offset
      type: number
      required: false
      default: 4
      description: On the content — distance in px from the trigger.
    - name: inset
      type: boolean
      required: false
      default: false
      description: >-
        On an item / submenu-trigger — left-pad to align with rows that
        have a leading indicator.

  events:
    - name: open-change
      payload: boolean
      description: Fired when the menu opens or closes (React `onOpenChange`).

  content:
    - name: default
      description: >-
        A `trigger` (typically a `ButtonMenu`) plus the `content` popup of
        `group`s containing `item`s — optionally with `shortcut` hints and
        nested submenus (`submenu-trigger` + `submenu-content`).

  methods: []

adapters:
  react:
    component: DropdownMenu
    status: implemented
    import: >-
      import { DropdownMenu, DropdownMenuTrigger, DropdownMenuContent,
      DropdownMenuGroup, DropdownMenuItem, DropdownMenuShortcut,
      DropdownMenuSub, DropdownMenuSubTrigger, DropdownMenuSubContent,
      ButtonMenu } from '@acronis-platform/ui-react'
    example: |
      <DropdownMenu>
        <DropdownMenuTrigger render={<ButtonMenu variant="secondary">Actions</ButtonMenu>} />
        <DropdownMenuContent>
          <DropdownMenuGroup>
            <DropdownMenuItem>
              Profile <DropdownMenuShortcut>⇧⌘P</DropdownMenuShortcut>
            </DropdownMenuItem>
            <DropdownMenuItem>Settings</DropdownMenuItem>
          </DropdownMenuGroup>
        </DropdownMenuContent>
      </DropdownMenu>

  vue:
    component: AvDropdownMenu
    status: planned
    import: "import { AvDropdownMenu, AvDropdownMenuItem } from '@acronis-platform/ui-vue'"
    example: |
      <AvDropdownMenu>
        <AvDropdownMenuItem>Profile</AvDropdownMenuItem>
      </AvDropdownMenu>

  web-components:
    element: ui-dropdown-menu
    status: planned
    import: "import '@acronis-platform/ui-wc/dropdown-menu'"
    example: |
      <ui-dropdown-menu>
        <ui-dropdown-menu-item>Profile</ui-dropdown-menu-item>
      </ui-dropdown-menu>
