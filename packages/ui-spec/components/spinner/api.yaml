spec_version: '1.0'
component: Spinner

contract:
  properties:
    - name: size
      type: "'sm' | 'md' | 'lg' | 'xl'"
      required: false
      default: "'md'"
      description: Diameter — sm 16 · md 24 · lg 32 · xl 48 (px).
  events: []
  methods: []

adapters:
  react:
    component: Spinner
    status: implemented
    import: "Not exported from '@acronis-platform/ui-react' — internal only, used by `Loading`/`Toast`."
    example: |
      // Internal usage only (packages/ui-react/src/components/ui/loading/loading.tsx,
      // .../toast/toast.tsx) — not importable by app code:
      <Spinner size="lg" className="text-muted-foreground" />
  vue:
    component: AvSpinner
    status: planned
    import: 'Not planned for public export — see AvLoading.'
    example: |
      // Internal only; apps should use AvLoading.
  web-components:
    element: ui-spinner
    status: planned
    import: 'Not planned for public export — see ui-loading.'
    example: |
      // Internal only; apps should use ui-loading.
