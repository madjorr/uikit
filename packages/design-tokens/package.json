{
  "name": "@acronis-platform/design-tokens",
  "version": "0.6.0",
  "description": "Acronis design tokens. DTCG-2025.10-conformant JSON.",
  "license": "MIT",
  "type": "module",
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "files": [
    "tiers/**",
    "schemas/**",
    "README.md",
    "CONTRIBUTING.md",
    "LICENSE"
  ],
  "exports": {
    "./package.json": "./package.json",
    "./tiers/primitives.json": "./tiers/primitives.json",
    "./tiers/semantic.json": "./tiers/semantic.json",
    "./tiers/components.json": "./tiers/components.json",
    "./schemas/*": "./schemas/*"
  },
  "scripts": {
    "build": "echo \"no build (JSON data package)\" && exit 0",
    "clean": "echo \"nothing to clean (JSON data package)\" && exit 0",
    "dev": "echo \"no dev server (JSON data package)\" && exit 0",
    "lint": "echo \"no lint (JSON data package; validated via test)\" && exit 0",
    "lint:fix": "echo \"no lint (JSON data package; validated via test)\" && exit 0",
    "test": "pnpm run validate",
    "test:watch": "echo \"no watch mode (run test)\" && exit 0",
    "typecheck": "echo \"no typecheck (JSON data package)\" && exit 0",
    "validate": "ajv compile -s schemas/tokens.schema.json --spec=draft2020 --strict=false && ajv validate -s schemas/tokens.schema.json -d \"tiers/primitives.json\" -d \"tiers/semantic.json\" -d \"tiers/components.json\" --spec=draft2020 --strict=false"
  },
  "keywords": [
    "design-tokens",
    "dtcg",
    "acronis"
  ],
  "devDependencies": {
    "ajv": "catalog:",
    "ajv-cli": "catalog:",
    "ajv-formats": "catalog:"
  }
}
