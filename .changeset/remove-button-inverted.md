---
'@acronis-platform/design-tokens': minor
---

Remove `Button.inverted` component tokens.

Deletes the 20 `Button.inverted.*` tokens — the inverted variant was removed from the React component in #544 and these token definitions are now unused.
