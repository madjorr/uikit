---
'@acronis-platform/ui-react': minor
---

Add `BarChart` — a typed recharts composition over the shared `Chart` primitives, with `orientation` (vertical / horizontal) and `layout` (grouped / stacked) variants, a themed tooltip/legend/axes/grid, and caller-supplied series colors. Initial version ported from the apps/demo `BarChartPlayground`; design + data-viz palette reconciliation pending.
