---
'@acronis-platform/ui-react': minor
---

`PieChart`: add a `centerLabel` prop for the donut hole — a headline `value` and an optional `label`, rendered as centered SVG text (donut shape only; ignored for a filled pie). The block stays centered on the donut whether or not a legend is shown. Themed from the existing `--ui-text-on-surface-primary` / `--ui-text-on-surface-secondary` tokens (`text-foreground` / `text-muted-foreground`).
