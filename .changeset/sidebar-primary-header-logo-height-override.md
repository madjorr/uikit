---
'@acronis-platform/ui-react': minor
---

`SidebarPrimaryHeader` now accepts `logoHeight`/`collapsedLogoHeight` props to override the design-system default logo slot height, for branded logos taller than the default 48px/32px.
